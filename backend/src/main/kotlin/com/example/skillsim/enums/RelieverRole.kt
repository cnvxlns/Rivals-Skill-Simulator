package com.example.skillsim.enums

/**
 * 중간 계투의 하위 역할.
 *
 * 게임의 투수 전략 화면이 중계 정원을 이 셋으로 나눠 배분한다.
 *
 * 저장하고 화면에 보여줄 뿐 **점수에는 반영하지 않는다.** 점수 엔진의 조건 토큰은
 * 슬롯 번호만 보고(`중계3_4_5`) 하위 역할에 해당하는 토큰이 CSV에 없기 때문이다.
 */
enum class RelieverRole {
    /** 승리조. */
    WIN,

    /** 추격조. */
    CHASE,

    /** 롱 릴리프. */
    LONG,
}
