-- 덱 종합 점수의 뜻이 바뀌었다.
--
-- 예전 total_score는 26명 스킬 점수의 단순 합이었다. 이제는 파트별 평균에 가중치를 매긴
-- 값이다(선발 0.4 + 계투 0.1 + 타자 0.5, 각 평균 × 10). 능력치 점수도 함께 들어간다.
--
-- 이 열은 목록 화면용 캐시일 뿐이고 상세 조회는 항상 다시 계산하므로, 비워 두면 각 덱을
-- 다시 저장할 때 새 기준으로 채워진다. 낡은 값을 남겨 두면 목록과 상세가 서로 다른 뜻의
-- 수를 나란히 보여 주게 된다.
update decks set total_score = null;

-- body는 건드리지 않는다. 새로 더한 필드는 전부 코틀린 기본값을 갖고 있어서 예전 jsonb가
-- 그대로 역직렬화된다(DeckRoster의 주석이 약속하는 계약이다). 그래서 예전 덱은 body_version
-- 1을 정직하게 유지하고, 다시 저장할 때만 2가 된다.
